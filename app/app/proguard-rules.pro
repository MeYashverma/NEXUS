# Elevon keeps ProGuard rules minimal: Compose and AndroidX ship consumer rules.
# HID, GATT and reflection-free code needs no extra keep rules.

# Keep line numbers in release crash reports.
-keepattributes SourceFile,LineNumberTable
